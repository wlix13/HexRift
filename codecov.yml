coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
    patch:
      default:
        target: 85%
        threshold: 5%

comment:
  layout: condensed_header, files, condensed_footer
  require_changes: true
